.filter-title-container
  .expand-button
    img.plus(src='../images/white-plus.png')
    img.minus(src='../images/white-minus.png')
  .filter-title= model.get("title")
ul.selected-items
.tooltip= model.get("tooltip")
.scroll-container
  ul.checkbox-list
    - items.each(function (item) {
      - var value = item.value;
      - var selected = model.isSelected(item.obj) ? "selected" : "";
      li(class=selected, data-key=item.value)
        .tick
        .title
          span= item.name
    - })